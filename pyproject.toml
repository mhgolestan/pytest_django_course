[project]
name = "pytest-django"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "djangorestframework>=3.16.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
